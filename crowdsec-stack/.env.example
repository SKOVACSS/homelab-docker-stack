# Optional stack - off by default. No .env here means deploy.ps1 skips
# this stack entirely, same pattern as email-stack. Run
# _scripts/enable-crowdsec.ps1 to turn it on - see SETUP.md for the
# manual Cloudflare steps that has to walk you through first.
TZ=UTC
